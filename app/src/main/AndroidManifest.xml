<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amazingme.activities">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="amazingme.activities.app.LandingPage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="amazingme.activities.games.ThreeTouchGame" />
        <activity android:name="amazingme.activities.app.MainActivity" />
        <activity android:name="amazingme.activities.app.RegisterActivity" />
        <activity android:name="amazingme.activities.app.LoginActivity" />
        <activity
            android:name="amazingme.activities.app.MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="amazingme.activities.app.ForgotPasswordActivity" />
        <activity android:name="amazingme.activities.app.UserProfileActivity"></activity>
    </application>

</manifest>